// Wait for the DOM to be fully loaded before running the script
document.addEventListener('DOMContentLoaded', () => {
    // This is a simple script to confirm that the file is linked correctly.
    // Based on the image, no complex JavaScript functionality is required.
    // You can add event listeners or other dynamic features here if needed.
    console.log("The contact page script is running!");
});
